module.exports = function(modules, configurations, classes, methods) {
    this.createRegularContext = function() {
        var progress = new classes.Progress();
        var context = new classes.Context();
    }
}